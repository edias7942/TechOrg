import { useState } from "react"

function Logout({teste}) {

    localStorage.clear()
    console.log(localStorage)
    return (
        <div>
            
        </div>
    )
}

export default Logout